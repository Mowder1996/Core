using NUnit.Framework;
using UnityEngine;

namespace MapModule.Tests
{
    [TestFixture]
    public class MapModuleTests
    {
        [Test]
        public void AddChainedTile()
        {
            // var tile1 = new HexagonalMapTile(Vector3.zero, Quaternion.identity);
            // var tile2 = new HexagonalMapTile(Vector3.right + Vector3.forward, Quaternion.identity * Quaternion.AngleAxis(180, Vector3.up));
            // var tile3 = new HexagonalMapTile(Vector3.left + Vector3.forward, Quaternion.identity * Quaternion.AngleAxis(0, Vector3.up));
            //
            // tile1.AddChainedTile(tile2);
            // tile1.AddChainedTile(tile3);
            
            
        }
    }
}